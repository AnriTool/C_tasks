#include <stdarg.h>
#ifndef FUNCTIONS_H
#define FUNCTIONS_H
//описываем функции которые мы использовали
int sum(int n, ...);
int max(int n, ...);
int min(int n, ...);
double average(int n, ...);
#endif
