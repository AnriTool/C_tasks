#include <stdbool.h> //Загружаем библиотеку для работы с bool
#ifndef FUNCTIONS_H
#define FUNCTIONS_H
//тут описываем функции которые мы использовали
bool exist(double a,double b,double c);
double per(double  a,double b,double c);
double sqr(double a,double b,double c);
#endif
